'use client'

// Placeholder - full implementation in Task 2
export function RsvpConfirmation({
  selectedDays: _selectedDays,
  guestName: _guestName,
}: {
  selectedDays: number[]
  guestName: string
}) {
  return <div>Confirmation placeholder</div>
}
