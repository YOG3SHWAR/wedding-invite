# Wedding Invite Project

## UI Work Requirement

When working on any UI-related task — creating components, modifying layouts, styling, building pages, or making visual changes — you MUST use the `frontend-design` skill. Invoke it via the Skill tool before generating UI code. This is a hard requirement, not a suggestion.
