---
phase: 03-rsvp-system
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/lib/firebase.ts
  - src/lib/rsvp.ts
  - .env.example
  - firestore.rules
  - src/test/setup.ts
  - src/__tests__/rsvp.test.ts
  - src/__tests__/rsvp-section.test.tsx
  - src/__tests__/rsvp-confirmation.test.tsx
  - src/__tests__/gifts-section.test.tsx
autonomous: true
requirements:
  - RSVP-01
  - RSVP-02
  - RSVP-03
  - RSVP-04

must_haves:
  truths:
    - "RSVP_DAYS and GIFT_ITEMS constants exist with correct structure"
    - "Firebase singleton initializes without errors"
    - "submitRsvp normalizes phone and calls setDoc with merge"
    - "Test stubs exist for all 4 RSVP requirements"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "RSVP_DAYS and GIFT_ITEMS constants"
      contains: "RSVP_DAYS"
    - path: "src/lib/firebase.ts"
      provides: "Firebase app singleton and Firestore db export"
      exports: ["db"]
    - path: "src/lib/rsvp.ts"
      provides: "submitRsvp function with phone normalization"
      exports: ["submitRsvp", "normalizePhone"]
    - path: "firestore.rules"
      provides: "Write-only Firestore security rules"
      contains: "allow create, update"
    - path: ".env.example"
      provides: "Firebase env var template"
      contains: "NEXT_PUBLIC_FIREBASE_API_KEY"
  key_links:
    - from: "src/lib/rsvp.ts"
      to: "src/lib/firebase.ts"
      via: "imports db"
      pattern: "import.*db.*from.*firebase"
    - from: "src/lib/rsvp.ts"
      to: "firebase/firestore"
      via: "setDoc with merge"
      pattern: "setDoc.*merge.*true"
---

<objective>
Install dependencies, create data constants (RSVP_DAYS, GIFT_ITEMS), Firebase config singleton, RSVP submit logic with phone normalization, Firestore security rules, and Wave 0 test stubs for all RSVP requirements.

Purpose: Establish the data layer and backend logic so UI plans can build against concrete contracts.
Output: Constants, Firebase integration, submit function, test scaffolds, .env.example template.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rsvp-system/03-RESEARCH.md

@src/lib/constants.ts
@src/test/setup.ts
</context>

<interfaces>
<!-- Existing constants the executor needs -->

From src/lib/constants.ts:
```typescript
export const EVENT_COLORS = {
  tilak: '#C41E3A',
  mehndi: '#228B22',
  sangeet: '#9B59B6',
  haldi: '#F4C430',
  shadi: '#800020',
} as const

export const COUPLE = {
  nameHindi: 'योगी & सुधा',
  nameEnglish: 'Yogi & Sudha',
  weddingDate: '28 April 2026',
} as const

export const EVENTS = [
  { name: 'Tilak', nameHindi: 'तिलक', date: '26 April 2026', time: '10:00 AM', venue: '...', dressCode: '...', colorKey: 'tilak' as const },
  // ... 5 events total
] as const
```

From src/test/setup.ts:
- Already mocks motion/react (Proxy-based) and next/image
- Uses @testing-library/jest-dom
- Need to ADD: firebase/firestore mock, canvas-confetti mock
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, create constants, Firebase config, and submit logic</name>
  <files>src/lib/constants.ts, src/lib/firebase.ts, src/lib/rsvp.ts, .env.example, firestore.rules</files>
  <action>
1. Install dependencies: `npm install firebase canvas-confetti && npm install -D @types/canvas-confetti`

2. Add RSVP_DAYS and GIFT_ITEMS to `src/lib/constants.ts` (append, do not modify existing exports):

   RSVP_DAYS array with 3 entries:
   - Day 1: label "Day 1", labelHindi "पहला दिन", date "26 April", events ["Tilak"], colorKey "tilak"
   - Day 2: label "Day 2", labelHindi "दूसरा दिन", date "27 April", events ["Mehndi", "Sangeet"], colorKey "mehndi"
   - Day 3: label "Day 3", labelHindi "तीसरा दिन", date "28 April", events ["Haldi", "Shadi"], colorKey "haldi"

   GIFT_ITEMS array with 4 placeholder items, each with: id, name, nameHindi, priceRange (string like "₹2,000 - ₹5,000"), image (placeholder path "/images/gifts/placeholder.webp"), buyUrl ("https://example.com")

3. Create `src/lib/firebase.ts` -- Firebase singleton pattern:
   - Import initializeApp, getApps from 'firebase/app' and getFirestore from 'firebase/firestore'
   - Read config from NEXT_PUBLIC_FIREBASE_* env vars
   - Use getApps().length === 0 guard for singleton
   - Export `db` (Firestore instance)

4. Create `src/lib/rsvp.ts`:
   - Export `normalizePhone(raw: string): string` -- strip non-digits, remove leading "91" if 12 digits, remove leading "0" if 11 digits, return 10-digit string
   - Export `validatePhone(raw: string): { valid: boolean; normalized: string; error?: string }` -- uses normalizePhone, returns Hindi+English error if not 10 digits: "कृपया 10 अंकों का फ़ोन नंबर दर्ज करें / Please enter a 10-digit phone number"
   - Export `submitRsvp(data: { name: string; phone: string; guestCount: number; days: number[] }): Promise<string>` -- normalizes phone, validates, calls setDoc(doc(db, 'rsvps', phoneId), {..., updatedAt: serverTimestamp()}, { merge: true }), returns phoneId
   - Export RsvpData type: { name: string; phone: string; guestCount: number; days: number[] }

5. Create `.env.example` with all NEXT_PUBLIC_FIREBASE_* var names (no values, just keys with comments)

6. Create `firestore.rules` -- write-only rules per research:
   - /rsvps/{phoneId}: allow create, update: if true; allow read, delete: if false
   - Everything else: deny all
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/rsvp.test.ts -x 2>/dev/null; node -e "const c = require('./src/lib/constants.ts'); console.log('skip')" 2>/dev/null || echo "Constants added"; test -f .env.example && test -f firestore.rules && test -f src/lib/firebase.ts && test -f src/lib/rsvp.ts && echo "All files exist"</automated>
  </verify>
  <done>firebase and canvas-confetti installed. RSVP_DAYS (3 days) and GIFT_ITEMS (4 placeholders) constants added. Firebase singleton in firebase.ts. submitRsvp with phone normalization in rsvp.ts. .env.example and firestore.rules created.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test stubs and mock setup</name>
  <files>src/test/setup.ts, src/__tests__/rsvp.test.ts, src/__tests__/rsvp-section.test.tsx, src/__tests__/rsvp-confirmation.test.tsx, src/__tests__/gifts-section.test.tsx</files>
  <action>
1. Update `src/test/setup.ts` -- ADD two new mocks (keep existing motion/react and next/image mocks intact):

   Mock firebase/firestore:
   ```
   vi.mock('firebase/firestore', () => ({
     getFirestore: vi.fn(() => ({})),
     doc: vi.fn((_db, collection, id) => ({ collection, id })),
     setDoc: vi.fn(() => Promise.resolve()),
     serverTimestamp: vi.fn(() => ({ _type: 'serverTimestamp' })),
   }))
   ```

   Mock firebase/app:
   ```
   vi.mock('firebase/app', () => ({
     initializeApp: vi.fn(() => ({})),
     getApps: vi.fn(() => []),
   }))
   ```

   Mock canvas-confetti:
   ```
   vi.mock('canvas-confetti', () => ({ default: vi.fn() }))
   ```

2. Create `src/__tests__/rsvp.test.ts` (unit tests for rsvp.ts logic):
   - Test normalizePhone: "9876543210" -> "9876543210", "+919876543210" -> "9876543210", "09876543210" -> "9876543210", "919876543210" -> "9876543210"
   - Test validatePhone: valid 10-digit returns { valid: true }, 8-digit returns { valid: false, error: contains "10" }
   - Test submitRsvp: mock setDoc, verify it is called with doc(db, 'rsvps', normalizedPhone) and { merge: true }

3. Create `src/__tests__/rsvp-section.test.tsx` with it.todo() stubs:
   - "renders RSVP section heading"
   - "renders 3 day cards with correct labels"
   - "day cards use correct accent colors"
   - "select all button selects all days"
   - "validates phone number on submit"
   - "shows loading spinner during submission"
   - "shows error message on failure"

4. Create `src/__tests__/rsvp-confirmation.test.tsx` with it.todo() stubs:
   - "renders thank you message in Hindi and English"
   - "shows summary cards for selected days"
   - "displays venue and dress code on summary cards"
   - "renders WhatsApp share button"
   - "renders back to top button"

5. Create `src/__tests__/gifts-section.test.tsx` with it.todo() stubs:
   - "renders blessing message in Hindi and English"
   - "renders gift cards from GIFT_ITEMS constant"
   - "each gift card shows name, price range, and buy link"
   - "gift card buy links open in new tab"
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All existing tests still pass. rsvp.test.ts has passing tests for normalizePhone, validatePhone, submitRsvp. Three component test files have it.todo() stubs. Firebase and canvas-confetti mocks in setup.ts.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` passes (existing + new rsvp logic tests)
- All files exist: firebase.ts, rsvp.ts, .env.example, firestore.rules
- constants.ts has RSVP_DAYS (3 items) and GIFT_ITEMS (4 items)
- Test stubs exist for all 4 requirements
</verification>

<success_criteria>
Data layer and test infrastructure complete. submitRsvp function tested with mocked Firestore. Constants define the contract for UI components. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsvp-system/03-00-SUMMARY.md`
</output>
