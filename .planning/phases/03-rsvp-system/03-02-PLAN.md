---
phase: 03-rsvp-system
plan: 02
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - src/components/sections/gifts-section.tsx
  - src/app/page.tsx
  - src/__tests__/gifts-section.test.tsx
autonomous: false
requirements:
  - RSVP-04

must_haves:
  truths:
    - "Gift section shows Hindi+English blessing message above gift cards"
    - "Gift cards display product name, price range, and buy link"
    - "RSVP section and gifts section are wired into main page"
    - "Full page builds without errors"
    - "All tests pass"
  artifacts:
    - path: "src/components/sections/gifts-section.tsx"
      provides: "Gift wishlist section with blessing and product cards"
      min_lines: 50
    - path: "src/app/page.tsx"
      provides: "Updated page with RSVP and gifts sections replacing placeholder"
  key_links:
    - from: "src/components/sections/gifts-section.tsx"
      to: "src/lib/constants.ts"
      via: "imports GIFT_ITEMS"
      pattern: "GIFT_ITEMS"
    - from: "src/app/page.tsx"
      to: "src/components/sections/rsvp-section.tsx"
      via: "renders RsvpSection"
      pattern: "RsvpSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/gifts-section.tsx"
      via: "renders GiftsSection"
      pattern: "GiftsSection"
---

<objective>
Build the registry/gifts section with blessing message and product cards, wire all new sections into the main page, and run a visual checkpoint for the complete RSVP flow.

Purpose: Complete Phase 3 by delivering the gifts section and integrating everything into the live page.
Output: gifts-section.tsx, updated page.tsx, visual verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rsvp-system/03-CONTEXT.md
@.planning/phases/03-rsvp-system/03-01-SUMMARY.md

@src/lib/constants.ts
@src/app/page.tsx
@src/components/sections/rsvp-section.tsx
@src/components/sections/rsvp-confirmation.tsx
@src/components/sections/section-divider.tsx
@src/components/ui/scroll-reveal.tsx
</context>

<interfaces>
<!-- Constants from Plan 00 -->
From src/lib/constants.ts:
```typescript
export const GIFT_ITEMS = [
  { id: 'gift-1', name: 'Placeholder Gift', nameHindi: 'उपहार', priceRange: '₹2,000 - ₹5,000', image: '/images/gifts/placeholder.webp', buyUrl: 'https://example.com' },
  // ... 4 items
] as const
```

<!-- Components from Plan 01 -->
From src/components/sections/rsvp-section.tsx:
```typescript
export function RsvpSection(): JSX.Element
// 'use client' component, self-contained with form + confirmation transition
```

From src/components/ui/scroll-reveal.tsx:
```typescript
export function ScrollReveal({ children, direction, delay, className }: ScrollRevealProps)
```

Current page.tsx structure:
- HeroSection -> SectionDivider -> OurStorySection -> SectionDivider -> EventTimeline -> SectionDivider -> PhotoGallery -> SectionDivider -> VideoSection -> SectionDivider -> RSVP placeholder
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build gifts section and wire page</name>
  <files>src/components/sections/gifts-section.tsx, src/app/page.tsx, src/__tests__/gifts-section.test.tsx</files>
  <action>
IMPORTANT: Use `frontend-design` skill for UI execution (hard constraint from user).

1. Create `src/components/sections/gifts-section.tsx`:

   This can be a server component (no interactivity needed) -- do NOT add 'use client' unless motion/react animations require it. If using ScrollReveal wrapper, make it 'use client'.

   **Blessing message header:**
   - Section id="gifts"
   - py-section-mobile md:py-section spacing
   - Hindi blessing: "आपका साथ ही हमारा सबसे बड़ा उपहार है" in font-hindi text-gold text-2xl md:text-3xl, centered
   - English: "Your presence is our greatest gift" in font-heading text-maroon text-xl, centered
   - Below: "If you'd like to bless us further:" in font-body text-maroon/70 italic, centered
   - Decorative gold divider line below (simple hr with border-gold/30)

   **Gift card grid:**
   - Grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6, max-w-4xl mx-auto
   - Map over GIFT_ITEMS constant
   - Each card: rounded-xl border border-gold/20 overflow-hidden bg-white shadow-sm hover:shadow-md transition-shadow
   - Card image area: aspect-square bg-cream/50 flex items-center justify-center (placeholder shows a gift emoji or decorative SVG since real images come later)
   - Card body: p-4
     - Name in font-heading text-maroon text-lg
     - Hindi name in font-hindi text-maroon/60 text-sm
     - Price range in font-body text-gold font-semibold
     - "Buy" link button: inline-block mt-3, bg-maroon text-white px-6 py-2 rounded-lg font-heading text-sm hover:bg-maroon/90
     - Buy link: target="_blank" rel="noopener noreferrer"
   - Wrap cards in ScrollReveal for staggered entrance

2. Update `src/app/page.tsx`:
   - Import RsvpSection from '@/components/sections/rsvp-section'
   - Import GiftsSection from '@/components/sections/gifts-section'
   - Replace the RSVP placeholder section with: RsvpSection component
   - Add SectionDivider after RsvpSection
   - Add GiftsSection after the divider
   - Order: ...VideoSection -> SectionDivider -> RsvpSection -> SectionDivider -> GiftsSection

3. Update `src/__tests__/gifts-section.test.tsx` -- replace it.todo() stubs with real tests:
   - Renders Hindi blessing "आपका साथ ही हमारा सबसे बड़ा उपहार है"
   - Renders English blessing "Your presence is our greatest gift"
   - Renders gift cards (check for GIFT_ITEMS[0].name in document)
   - Gift cards have buy links with target="_blank"
   - Renders correct number of gift cards (4)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -30 && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Gifts section renders blessing message and product cards from GIFT_ITEMS. Page.tsx has RsvpSection and GiftsSection replacing the placeholder. Build succeeds. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete RSVP flow and gifts section</name>
  <action>
Visual checkpoint -- verify the complete RSVP system and gifts section on desktop and mobile.

What was built: Complete RSVP system with day-wise form (accent-colored cards, select-all button, phone validation, loading spinner), full-screen confirmation with gold confetti and event summaries, WhatsApp share button, and gift wishlist section with blessing message and product cards. All wired into the main page after the video section.

How to verify:
1. Run `npm run dev` and open http://localhost:3000
2. Scroll to RSVP section (after video):
   - Verify 3 day cards with distinct accent colors (red/green/yellow)
   - Tap "Sabhi Din / All Days" -- all 3 cards should highlight
   - Tap individual cards to toggle selection
   - Fill name and phone (any 10 digits), set guest count
   - Submit will fail without Firebase credentials -- that is expected
   - Verify the form layout looks royal/elegant, large touch targets
3. Scroll to Gifts section:
   - Verify Hindi+English blessing message
   - Verify 4 placeholder gift cards in grid
   - Verify buy links open in new tab
4. Check mobile view (Chrome DevTools, 375px width):
   - Day cards stack vertically, large and tappable
   - Form fields are large (h-14+)
   - Gift cards: 1 column on mobile
5. Optional (if Firebase configured): Submit RSVP and verify confetti + confirmation screen

Note: Firebase credentials needed for full submit flow. If not configured yet, verify the UI layout, day cards, form validation, and gift section only.
  </action>
  <verify>User visually approves the RSVP form layout, day cards, gifts section on desktop and mobile</verify>
  <done>User types "approved" confirming the RSVP flow and gifts section look correct</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx next build` -- builds without errors
- Visual inspection of RSVP form, day cards, gifts section on desktop and mobile
</verification>

<success_criteria>
Gifts section with blessing and product cards is live. RSVP and gifts sections are integrated into the main page. Build passes. User visually approves the RSVP flow and gifts section layout.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsvp-system/03-02-SUMMARY.md`
</output>
