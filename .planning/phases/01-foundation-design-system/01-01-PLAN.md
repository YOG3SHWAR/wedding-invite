---
phase: 01-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/opengraph-image.jpg
  - src/lib/fonts.ts
  - src/lib/constants.ts
  - src/components/ui/scroll-reveal.tsx
  - src/components/providers/animation-provider.tsx
  - src/__tests__/metadata.test.ts
  - src/__tests__/fonts.test.ts
  - src/__tests__/scroll-reveal.test.tsx
autonomous: true
requirements:
  - HERO-03
  - CONT-02
  - ANIM-02
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "Next.js project builds and runs without errors"
    - "Hindi text renders in Yatra One font, English headings in Playfair Display, English body in Cormorant Garamond — no flash of invisible text"
    - "Tailwind utility classes (bg-maroon, text-gold, bg-cream, text-emerald) apply correct colors"
    - "ScrollReveal component fades elements in when scrolled into view"
    - "ScrollReveal respects prefers-reduced-motion"
    - "OG meta tags export correct title, description, and image for WhatsApp preview"
    - "Page layout is mobile-first responsive (readable at 375px width)"
    - "Build output is lean — no unnecessary client JS bundles"
  artifacts:
    - path: "src/lib/fonts.ts"
      provides: "Font configuration with Yatra One, Playfair Display, Cormorant Garamond"
      exports: ["yatraOne", "playfairDisplay", "cormorantGaramond"]
    - path: "src/app/globals.css"
      provides: "Design tokens via Tailwind v4 @theme — colors, fonts, spacing"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, metadata, OG tags"
      exports: ["metadata", "default"]
    - path: "src/components/ui/scroll-reveal.tsx"
      provides: "Reusable scroll animation wrapper"
      exports: ["ScrollReveal"]
    - path: "src/components/providers/animation-provider.tsx"
      provides: "MotionConfig wrapper with reducedMotion support"
      exports: ["AnimationProvider"]
    - path: "src/app/page.tsx"
      provides: "Placeholder page demonstrating fonts, colors, scroll animation"
    - path: "src/lib/constants.ts"
      provides: "Event accent colors, content strings"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with React/JSX support"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/fonts.ts"
      via: "imports font variables, applies to html className"
      pattern: "yatraOne\\.variable.*playfairDisplay\\.variable.*cormorantGaramond\\.variable"
    - from: "src/app/globals.css"
      to: "src/lib/fonts.ts"
      via: "@theme references CSS variables set by next/font"
      pattern: "--font-hindi.*--font-heading.*--font-body"
    - from: "src/app/page.tsx"
      to: "src/components/ui/scroll-reveal.tsx"
      via: "imports and uses ScrollReveal wrapper"
      pattern: "import.*ScrollReveal"
    - from: "src/app/layout.tsx"
      to: "src/components/providers/animation-provider.tsx"
      via: "wraps children in AnimationProvider for reducedMotion"
      pattern: "AnimationProvider"
---

<objective>
Scaffold the entire Next.js project from scratch and establish the complete design system — Tailwind v4 theme tokens (colors, fonts, spacing), bilingual font loading (Hindi/English via next/font), reusable ScrollReveal animation wrapper with reduced-motion support, WhatsApp OG meta tags, a placeholder page demonstrating all patterns, and unit tests for key contracts.

Purpose: This is the foundation every subsequent phase builds on. Getting fonts, colors, animation patterns, and performance right here means Phase 2-4 work is consistent and fast.
Output: A building, running, tested Next.js project with the complete design system.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with design system, fonts, OG metadata, and test infrastructure</name>
  <files>
    package.json, tsconfig.json, next.config.ts, vitest.config.ts,
    src/app/globals.css, src/app/layout.tsx, src/lib/fonts.ts, src/lib/constants.ts,
    src/components/providers/animation-provider.tsx
  </files>
  <action>
    1. **Scaffold project:**
       ```bash
       npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias
       ```
       Use `@/*` path alias if prompted. Accept all defaults.

    2. **Install dependencies:**
       ```bash
       npm install motion
       npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom
       ```

    3. **Create `vitest.config.ts`** in project root:
       - Import `@vitejs/plugin-react`
       - Set environment to `jsdom`
       - Set `globals: true` for auto-imports of describe/it/expect
       - Add path alias `@` -> `./src`

    4. **Create `src/lib/fonts.ts`:**
       - Import `Yatra_One` from `next/font/google` — weight `'400'`, subsets `['devanagari', 'latin']`, variable `'--font-hindi'`, display `'swap'`
       - Import `Playfair_Display` from `next/font/google` — subsets `['latin']`, variable `'--font-heading'`, display `'swap'`
       - Import `Cormorant_Garamond` from `next/font/google` — subsets `['latin']`, weight `['400', '500', '600', '700']`, variable `'--font-body'`, display `'swap'`
       - Export all three font instances

    5. **Create `src/lib/constants.ts`:**
       - Export `EVENT_COLORS` object: `{ tilak: '#C41E3A', mehndi: '#228B22', sangeet: '#9B59B6', haldi: '#F4C430', shadi: '#800020' }`
       - Export `COUPLE` object: `{ nameHindi: 'योगी & सुधा', nameEnglish: 'Yogi & Sudha', weddingDate: '28 April 2026' }`
       - Export `EVENTS` array with all 5 events: name, nameHindi, date, color key

    6. **Replace `src/app/globals.css`** entirely with Tailwind v4 design tokens:
       - `@import "tailwindcss";`
       - `@theme` block with:
         - Font mappings: `--font-hindi: var(--font-hindi)`, `--font-heading: var(--font-heading)`, `--font-body: var(--font-body)`
         - Primary colors: maroon (#800020), maroon-light (#A0334D), maroon-dark (#600018), gold (#D4AF37), gold-light (#E8CC6E), gold-dark (#B8941F), emerald (#046307), emerald-light (#0A8A0F), cream (#FFF8F0), cream-dark (#F5E6D3)
         - Event accents: tilak (#C41E3A), mehndi (#228B22), sangeet (#9B59B6), haldi (#F4C430), shadi (#800020)
         - Section spacing: `--spacing-section: 6rem`, `--spacing-section-mobile: 3rem`
       - Base body styles: `font-family: var(--font-body), 'Georgia', serif`, `background-color: var(--color-cream)`, `color: var(--color-maroon-dark)`
       Remove ALL default Next.js/Tailwind template CSS (the grid layout, dark mode styles, etc.)

    7. **Create `src/components/providers/animation-provider.tsx`:**
       - Mark `'use client'`
       - Import `MotionConfig` from `motion/react`
       - Wrap children in `<MotionConfig reducedMotion="user">`
       - Export as named `AnimationProvider`

    8. **Replace `src/app/layout.tsx`** entirely:
       - Import fonts from `@/lib/fonts`
       - Import `AnimationProvider` from `@/components/providers/animation-provider`
       - Import `./globals.css`
       - Export `metadata` object with:
         - `metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000')`
         - `title: 'Yogi & Sudha Wedding'`
         - `description: 'Join us for our wedding celebration! 28 April 2026'`
         - `openGraph` with title, description, type `'website'`, images array with `/opengraph-image.jpg` (1200x630)
       - Default export `RootLayout` that renders `<html lang="en" className={font variables joined}>` with `<body className="bg-cream text-maroon-dark font-body antialiased">` wrapping `<AnimationProvider>{children}</AnimationProvider>`

    9. **Create placeholder OG image:**
       - Create a simple 1200x630 placeholder image at `src/app/opengraph-image.jpg` — use a solid maroon background with gold text overlay saying "Yogi & Sudha | 28 April 2026". This can be generated programmatically or as a simple placeholder. If generating is complex, create a minimal valid JPEG file and note it needs replacement with actual couple photo later.
       - Alternative: If generating an image programmatically is not feasible in this context, create `src/app/opengraph-image.tsx` using Next.js ImageResponse API to dynamically generate the OG image with maroon background and gold text.

    10. **Clean up scaffolded files:**
        - Remove `src/app/favicon.ico` default Next.js favicon (or replace with a relevant one)
        - Remove any default SVG assets from `public/`
        - Remove default page content from `page.tsx` (will be replaced in Task 2)
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npm run build</automated>
  </verify>
  <done>
    - Next.js project builds without errors
    - `src/lib/fonts.ts` exports 3 font instances with correct CSS variables
    - `src/app/globals.css` contains @theme with all color and font tokens
    - `src/app/layout.tsx` exports metadata with OG tags and renders with font variables + AnimationProvider
    - Vitest is installed and configured
    - No default Next.js template content remains
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ScrollReveal component and placeholder page demonstrating all design patterns</name>
  <files>
    src/components/ui/scroll-reveal.tsx, src/app/page.tsx
  </files>
  <action>
    1. **Create `src/components/ui/scroll-reveal.tsx`:**
       - Mark `'use client'`
       - Import `motion` and `type Variants` from `motion/react`
       - Import `type ReactNode` from `react`
       - Define `variants` record with 4 animation presets:
         - `fadeUp`: hidden `{ opacity: 0, y: 40 }` -> visible `{ opacity: 1, y: 0 }`
         - `fadeIn`: hidden `{ opacity: 0 }` -> visible `{ opacity: 1 }`
         - `slideLeft`: hidden `{ opacity: 0, x: -60 }` -> visible `{ opacity: 1, x: 0 }`
         - `slideRight`: hidden `{ opacity: 0, x: 60 }` -> visible `{ opacity: 1, x: 0 }`
       - Props interface: `children: ReactNode`, `variant?: keyof typeof variants` (default `'fadeUp'`), `delay?: number` (default 0), `duration?: number` (default 0.6), `className?: string`
       - Render `<motion.div>` with `initial="hidden"`, `whileInView="visible"`, `viewport={{ once: true, margin: '-50px' }}`, transition with duration/delay/ease `'easeOut'`
       - Export as named `ScrollReveal`

    2. **Replace `src/app/page.tsx`** with a demonstration page:
       - Import `ScrollReveal` from `@/components/ui/scroll-reveal`
       - Build a single-page layout that demonstrates ALL design system patterns:

       **Section 1 — Hero placeholder (full viewport height):**
       - Centered content with generous padding
       - Hindi heading: `योगी & सुधा` in `font-hindi text-gold` with dramatic size (`text-6xl md:text-8xl lg:text-9xl`)
       - English heading: `Yogi & Sudha` in `font-heading text-maroon` with large size (`text-3xl md:text-5xl lg:text-6xl`)
       - Date: `28 April 2026` in `font-body text-maroon/80`
       - Gold decorative divider line below (simple `<hr>` with `border-gold` or a styled `<div>`)
       - Apply ornate border: a `border-2 border-gold` frame or similar decorative framing around the hero area

       **Section 2 — Design system showcase (scroll to see):**
       - Wrap in `<ScrollReveal>` (default fadeUp)
       - Show color swatches: maroon, gold, emerald, cream — small colored boxes with labels
       - Show event accent colors: tilak, mehndi, sangeet, haldi, shadi
       - All in a responsive grid (`grid grid-cols-2 md:grid-cols-5 gap-4`)

       **Section 3 — Typography showcase:**
       - Wrap in `<ScrollReveal variant="slideLeft">`
       - Hindi sample text in `font-hindi` with heading sizing
       - English heading sample in `font-heading`
       - English body sample in `font-body` with a paragraph of text
       - Demonstrate the dramatic type scale

       **Section 4 — Animation variants showcase:**
       - Four boxes, each wrapped in `<ScrollReveal>` with a different variant (fadeUp, fadeIn, slideLeft, slideRight)
       - Each box: `bg-cream-dark border border-gold rounded-lg p-6` with the variant name inside
       - Stagger delays: 0, 0.1, 0.2, 0.3

       **Section 5 — Responsive test:**
       - A section with `px-4 md:px-8 lg:px-16` showing mobile-first responsive padding
       - Content in a `max-w-4xl mx-auto` container
       - Text: "This section tests responsive layout. View at different screen widths."

       All sections should have generous vertical spacing (`py-16 md:py-24` or using the section spacing token).
       Use `min-h-screen` on hero section only. Other sections flow naturally.
       Mobile-first: all sizing starts from mobile and scales up via `md:` and `lg:` prefixes.
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npm run build && npm run dev -- --port 3333 &amp; sleep 3 && curl -s -o /dev/null -w "%{http_code}" http://localhost:3333 && kill %1</automated>
  </verify>
  <done>
    - ScrollReveal component exports with 4 animation variants
    - Placeholder page renders with Hindi + English text in correct fonts
    - All Tailwind color utilities (bg-maroon, text-gold, bg-cream, etc.) apply correctly
    - ScrollReveal animations trigger on scroll
    - Page is mobile-first responsive (readable at 375px, enhanced at md/lg)
    - No default Next.js template content visible
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write unit tests for OG metadata, font configuration, and ScrollReveal component</name>
  <files>
    src/__tests__/metadata.test.ts, src/__tests__/fonts.test.ts, src/__tests__/scroll-reveal.test.tsx
  </files>
  <behavior>
    - Test: OG metadata exports title "Yogi & Sudha Wedding"
    - Test: OG metadata exports description containing "28 April 2026"
    - Test: OG metadata exports openGraph.images with width 1200 and height 630
    - Test: Font config exports yatraOne with variable "--font-hindi"
    - Test: Font config exports playfairDisplay with variable "--font-heading"
    - Test: Font config exports cormorantGaramond with variable "--font-body"
    - Test: ScrollReveal renders children
    - Test: ScrollReveal applies motion props (initial, whileInView)
    - Test: ScrollReveal accepts variant prop
  </behavior>
  <action>
    1. **Create `src/__tests__/metadata.test.ts`:**
       - Import `metadata` from `@/app/layout` (may need to handle the default export separately)
       - Note: Since layout.tsx exports both `metadata` (named) and the component (default), import just the metadata constant
       - Test "exports correct title": `expect(metadata.title).toBe('Yogi & Sudha Wedding')`
       - Test "exports correct description": `expect(metadata.description).toContain('28 April 2026')`
       - Test "exports openGraph images": verify `metadata.openGraph.images[0]` has `width: 1200`, `height: 630`
       - Test "exports openGraph title": `expect(metadata.openGraph.title).toBe('Yogi & Sudha Wedding')`

    2. **Create `src/__tests__/fonts.test.ts`:**
       - Import `{ yatraOne, playfairDisplay, cormorantGaramond }` from `@/lib/fonts`
       - Test "yatraOne has correct CSS variable": `expect(yatraOne.variable).toBe('--font-hindi')` (note: `next/font` may prefix/transform variable names — if so, check it contains `font-hindi`)
       - Test "playfairDisplay has correct CSS variable": check variable contains `font-heading`
       - Test "cormorantGaramond has correct CSS variable": check variable contains `font-body`
       - Test "all fonts have swap display": check each font's `style` property or that the configuration was set correctly
       - Note: `next/font/google` functions may not work in Vitest without mocking. If direct import fails, mock `next/font/google` to return objects with the expected shape. Create a `__mocks__/next/font/google.ts` or use `vi.mock('next/font/google', ...)` to return `{ variable, className, style }` objects.

    3. **Create `src/__tests__/scroll-reveal.test.tsx`:**
       - Import `render, screen` from `@testing-library/react`
       - Import `ScrollReveal` from `@/components/ui/scroll-reveal`
       - Mock `motion/react` to render a plain div that passes through props (motion.div in test env):
         ```typescript
         vi.mock('motion/react', () => ({
           motion: { div: ({ children, ...props }: any) => <div data-testid="motion-div" {...props}>{children}</div> },
           MotionConfig: ({ children }: any) => children,
         }))
         ```
       - Test "renders children": render `<ScrollReveal><span>Test</span></ScrollReveal>`, expect `screen.getByText('Test')` to be in the document
       - Test "renders with default variant": verify the motion div receives initial and whileInView props
       - Test "accepts className prop": render with `className="test-class"`, verify it's applied

    4. **Run tests and ensure all pass:**
       ```bash
       npx vitest run --reporter=verbose
       ```
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - All 3 test files exist and pass
    - OG metadata tests verify title, description, image dimensions
    - Font tests verify CSS variable names
    - ScrollReveal tests verify rendering and prop passing
    - `npx vitest run` exits with 0
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run the full verification suite:

```bash
# Build succeeds
npm run build

# All tests pass
npx vitest run --reporter=verbose

# Dev server starts and responds
npm run dev -- --port 3333 &
sleep 3
curl -s http://localhost:3333 | grep -q "Yogi" && echo "PASS: Page renders" || echo "FAIL: Page not rendering"
kill %1
```
</verification>

<success_criteria>
1. `npm run build` succeeds with zero errors
2. `npx vitest run` passes all tests (metadata, fonts, scroll-reveal)
3. Dev server renders the placeholder page with Hindi text in Yatra One, English headings in Playfair Display, body in Cormorant Garamond
4. Tailwind utility classes `bg-maroon`, `text-gold`, `bg-cream`, `text-emerald` apply correct hex colors
5. ScrollReveal elements animate on scroll (fade-in visible when scrolled into view)
6. OG metadata exports title "Yogi & Sudha Wedding" with 1200x630 image
7. Page is mobile-first — content is readable at 375px viewport width
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
