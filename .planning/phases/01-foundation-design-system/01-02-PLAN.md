---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/page.tsx
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - ANIM-02

must_haves:
  truths:
    - "Page loads under 3 seconds on throttled 4G in Chrome DevTools"
    - "Layout is responsive and readable at 375px mobile width"
    - "Scroll animations play smoothly and trigger at correct scroll positions"
    - "Color theme matches royal wedding aesthetic — gold, maroon, emerald on cream"
    - "Hindi and English fonts render without flash of invisible text"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Polished placeholder page (may have minor adjustments from visual review)"
  key_links:
    - from: "build output"
      to: "performance budget"
      via: "Lighthouse audit"
      pattern: "Performance.*[89][0-9]|100"
---

<objective>
Visually verify the design system and performance foundation established in Plan 01. Run Lighthouse audit on throttled 4G to confirm the 3-second load budget. Check responsive layout at mobile breakpoints. Verify the royal wedding aesthetic renders correctly.

Purpose: Catch visual or performance issues before building content sections on this foundation.
Output: Verified, performant design system ready for Phase 2.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build, tests, and automated performance checks</name>
  <files>src/app/page.tsx</files>
  <action>
    1. Run the full build and test suite:
       ```bash
       npm run build
       npx vitest run --reporter=verbose
       ```

    2. Check the build output size — verify the JS bundle is minimal:
       - Look at the build output for page sizes
       - First Load JS should be under 100KB (ideally under 80KB)
       - If Motion is appearing in the server bundle, fix imports to only use in 'use client' components

    3. Start the dev server and verify the page responds:
       ```bash
       npm run dev -- --port 3333
       ```
       Verify with curl that the page returns HTML containing "Yogi" and the font CSS variables.

    4. If any build warnings or test failures exist, fix them now:
       - Fix any Tailwind v4 @theme issues (namespace conventions)
       - Fix any font loading issues (display: swap, subsets)
       - Fix any Motion import issues (must be in 'use client' components)

    5. Review the page.tsx for any adjustments needed:
       - Ensure all sections have adequate spacing for the "dramatic" feel
       - Verify gold decorative elements are present
       - Make minor CSS adjustments if colors or spacing look off based on the class application
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npm run build && npx vitest run</automated>
  </verify>
  <done>
    - Build succeeds with no warnings
    - All tests pass
    - First Load JS under 100KB
    - Dev server responds with page content
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and performance verification of design system</name>
  <action>
    Present the running application to the user for visual verification. The dev server should already be running from Task 1. Guide the user through checking fonts, colors, animations, responsive layout, and performance.
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npm run build</automated>
  </verify>
  <done>User approves the visual design, responsive layout, and performance of the foundation</done>
  <what-built>
    Complete design system foundation: Next.js project with royal wedding color theme (maroon, gold, emerald on cream), Hindi text in Yatra One font, English headings in Playfair Display, body text in Cormorant Garamond, ScrollReveal scroll animations, and WhatsApp OG metadata.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:3000 in Chrome

    **Visual checks:**
    3. Verify the Hindi text renders in a decorative/calligraphic font (Yatra One), NOT a system font
    4. Verify English heading "Yogi & Sudha" appears in an elegant serif font (Playfair Display)
    5. Verify body text appears in a refined serif font (Cormorant Garamond)
    6. Verify color theme: gold headings, maroon text, cream background — royal wedding card aesthetic
    7. Scroll down to see sections animate in (fade, slide effects)

    **Responsive check:**
    8. Open Chrome DevTools (F12) -> Toggle device toolbar (Ctrl+Shift+M)
    9. Set viewport to 375px width (iPhone SE)
    10. Verify text is readable, no horizontal overflow, layout stacks vertically

    **Performance check:**
    11. In Chrome DevTools -> Lighthouse tab
    12. Select "Mobile" and "Performance" category
    13. Run audit — verify Performance score is 80+ (on local, production will be similar or better)

    **OG tag check:**
    14. View page source (Ctrl+U) and search for "og:" — verify title is "Yogi & Sudha Wedding" and image path is present
  </how-to-verify>
  <resume-signal>Type "approved" if the design system looks correct, or describe any issues (wrong fonts, colors off, animations not working, etc.)</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
- Build passes, all tests green
- Visual design matches royal wedding aesthetic
- Fonts render correctly (Hindi decorative, English serif)
- Scroll animations work
- Page is responsive at 375px
- Lighthouse performance score 80+
- OG meta tags present in page source
</verification>

<success_criteria>
1. User approves the visual design (fonts, colors, animations)
2. Build succeeds and all unit tests pass
3. Lighthouse mobile performance score >= 80
4. Page is readable and well-laid-out at 375px viewport
5. No flash of invisible text on font loading
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
