---
phase: 02-presentation-sections
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/test/setup.ts
  - src/components/sections/__tests__/hero-section.test.tsx
  - src/components/sections/__tests__/hero-countdown.test.tsx
  - src/components/sections/__tests__/event-timeline.test.tsx
  - src/components/sections/__tests__/event-card.test.tsx
  - src/components/sections/__tests__/photo-gallery.test.tsx
  - src/components/sections/__tests__/our-story-section.test.tsx
  - src/components/sections/__tests__/video-section.test.tsx
  - src/components/ui/__tests__/scroll-reveal.test.tsx
  - vitest.config.ts
autonomous: true
requirements: [HERO-01, HERO-02, EVNT-01, EVNT-02, EVNT-03, CONT-01, CONT-03, CONT-04, ANIM-01]

must_haves:
  truths:
    - "All 8 test stub files exist and are importable"
    - "Shared test setup mocks motion/react and next/image"
    - "Running vitest shows 8 test files with pending/skipped tests (no failures)"
  artifacts:
    - path: "src/test/setup.ts"
      provides: "Shared mocks for motion/react and next/image"
      contains: "vi.mock"
    - path: "src/components/sections/__tests__/hero-section.test.tsx"
      provides: "Test stubs for HERO-01"
      contains: "describe"
    - path: "src/components/sections/__tests__/hero-countdown.test.tsx"
      provides: "Test stubs for HERO-02"
      contains: "describe"
    - path: "src/components/sections/__tests__/event-timeline.test.tsx"
      provides: "Test stubs for EVNT-01"
      contains: "describe"
    - path: "src/components/sections/__tests__/event-card.test.tsx"
      provides: "Test stubs for EVNT-02, EVNT-03"
      contains: "describe"
    - path: "src/components/sections/__tests__/photo-gallery.test.tsx"
      provides: "Test stubs for CONT-01"
      contains: "describe"
    - path: "src/components/sections/__tests__/our-story-section.test.tsx"
      provides: "Test stubs for CONT-03"
      contains: "describe"
    - path: "src/components/sections/__tests__/video-section.test.tsx"
      provides: "Test stubs for CONT-04"
      contains: "describe"
    - path: "src/components/ui/__tests__/scroll-reveal.test.tsx"
      provides: "Test stubs for ANIM-01"
      contains: "describe"
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles array"
      pattern: "setupFiles.*setup"
---

<objective>
Create all Wave 0 test stub files and shared test setup so that implementation plans (02-01, 02-02) can verify behavioral correctness via unit tests rather than relying solely on type-checking or full builds.

Purpose: Establish the Nyquist-compliant testing foundation. Every component created in Wave 1 and Wave 2 will have a corresponding test file ready to run. This enables fast (<15s) per-task verification.

Output: 8 test stub files with `describe` blocks and `it.todo()` placeholders for each behavior, plus a shared setup file that mocks `motion/react` and `next/image`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-presentation-sections/02-CONTEXT.md
@.planning/phases/02-presentation-sections/02-RESEARCH.md
@.planning/phases/02-presentation-sections/02-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure -->

From vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

From src/components/ui/scroll-reveal.tsx:
```typescript
export function ScrollReveal({
  children,
  variant = 'fadeUp',  // 'fadeUp' | 'fadeIn' | 'slideLeft' | 'slideRight'
  delay = 0,
  duration = 0.6,
  className,
}: ScrollRevealProps)
```

Animation library: `motion/react` (NOT framer-motion)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared test setup and update vitest config</name>
  <files>src/test/setup.ts, vitest.config.ts</files>
  <action>
1. **Create `src/test/setup.ts`:** This file provides shared mocks used by all component tests:

   - Mock `motion/react`: Use `vi.mock('motion/react', ...)` to replace `motion.div`, `motion.span`, `motion.section`, etc. with plain HTML elements that forward props and children. The mock should handle both named exports (`motion`, `AnimatePresence`) and the `motion.X` proxy pattern. Each `motion.X` should render as its corresponding HTML element (e.g., `motion.div` renders a `<div>`). Pass through `className`, `children`, `style`, `onClick`, and data attributes. Ignore animation-specific props (`initial`, `animate`, `exit`, `variants`, `whileInView`, `viewport`, `transition`).

   - Mock `next/image`: Use `vi.mock('next/image', ...)` to replace the Image component with a plain `<img>` element that passes through `src`, `alt`, `width`, `height`, `className`, and `loading` props.

   - Import `@testing-library/jest-dom` for extended matchers (toBeInTheDocument, toHaveClass, etc.). Install if not present: `npm install -D @testing-library/jest-dom`.

2. **Update `vitest.config.ts`:** Add the setup file to `setupFiles`:
   ```
   setupFiles: ['./src/test/setup.ts'],
   ```
   Leave all other config unchanged.
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Setup file exists with motion/react and next/image mocks. vitest.config.ts references the setup file. Running vitest produces no import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 8 test stub files with behavior placeholders</name>
  <files>src/components/sections/__tests__/hero-section.test.tsx, src/components/sections/__tests__/hero-countdown.test.tsx, src/components/sections/__tests__/event-timeline.test.tsx, src/components/sections/__tests__/event-card.test.tsx, src/components/sections/__tests__/photo-gallery.test.tsx, src/components/sections/__tests__/our-story-section.test.tsx, src/components/sections/__tests__/video-section.test.tsx, src/components/ui/__tests__/scroll-reveal.test.tsx</files>
  <action>
Create 8 test files. Each file uses `describe` + `it.todo()` for now. The stubs document WHAT will be tested. Implementation plans (02-01, 02-02) will fill in test bodies when they build the corresponding components.

**IMPORTANT:** Use `it.todo('description')` for tests whose components do not exist yet. This ensures vitest reports them as "todo" (not failures). Do NOT import components that do not exist yet -- put imports in comments that the implementation plan will uncomment.

1. **`src/components/sections/__tests__/hero-section.test.tsx`** (HERO-01):
   ```
   // import { render, screen } from '@testing-library/react'
   // import { HeroSection } from '../hero-section'

   describe('HeroSection', () => {
     it.todo('renders Hindi blessing text "शुभ विवाह"')
     it.todo('renders couple names in Hindi from COUPLE constant')
     it.todo('renders couple names in English from COUPLE constant')
     it.todo('renders wedding date')
     it.todo('includes HeroCountdown component')
   })
   ```

2. **`src/components/sections/__tests__/hero-countdown.test.tsx`** (HERO-02):
   ```
   describe('HeroCountdown', () => {
     it.todo('displays four countdown boxes (days, hours, minutes, seconds)')
     it.todo('shows correct time remaining for a future date')
     it.todo('shows all zeros when target date has passed')
     it.todo('updates every second via setInterval')
     it.todo('cleans up interval on unmount')
   })
   ```

3. **`src/components/sections/__tests__/event-timeline.test.tsx`** (EVNT-01):
   ```
   describe('EventTimeline', () => {
     it.todo('renders all 5 events from EVENTS constant')
     it.todo('renders section heading "Wedding Events"')
     it.todo('renders Hindi heading "शुभ कार्यक्रम"')
     it.todo('wraps each event card in ScrollReveal')
   })
   ```

4. **`src/components/sections/__tests__/event-card.test.tsx`** (EVNT-02, EVNT-03):
   ```
   describe('EventCard', () => {
     it.todo('renders event name in Hindi and English')
     it.todo('renders date and time')
     it.todo('renders venue with Google Maps link')
     it.todo('renders dress code suggestion')
     it.todo('renders event description')
     it.todo('applies distinct accent color border from EVENT_COLORS')
   })
   ```

5. **`src/components/sections/__tests__/photo-gallery.test.tsx`** (CONT-01):
   ```
   describe('PhotoGallery', () => {
     it.todo('renders gallery images from placeholder data')
     it.todo('opens lightbox when image is clicked')
     it.todo('closes lightbox when close is triggered')
     it.todo('renders section heading "Gallery" and Hindi heading')
   })
   ```

6. **`src/components/sections/__tests__/our-story-section.test.tsx`** (CONT-03):
   ```
   describe('OurStorySection', () => {
     it.todo('renders all story milestones from STORY_MILESTONES')
     it.todo('renders section heading "Our Story"')
     it.todo('renders Hindi heading "हमारी कहानी"')
     it.todo('renders milestone title and description for each milestone')
     it.todo('wraps milestones in ScrollReveal for scroll animations')
   })
   ```

7. **`src/components/sections/__tests__/video-section.test.tsx`** (CONT-04):
   ```
   describe('VideoSection', () => {
     it.todo('renders thumbnail with play button')
     it.todo('does not render iframe initially')
     it.todo('renders iframe with autoplay after click')
     it.todo('renders section heading "Our Film"')
   })
   ```

8. **`src/components/ui/__tests__/scroll-reveal.test.tsx`** (ANIM-01):
   ```
   import { render, screen } from '@testing-library/react'
   import { ScrollReveal } from '../scroll-reveal'

   describe('ScrollReveal', () => {
     it('renders children', () => {
       render(<ScrollReveal><span data-testid="child">hello</span></ScrollReveal>)
       expect(screen.getByTestId('child')).toBeInTheDocument()
     })

     it('accepts variant prop', () => {
       const { container } = render(
         <ScrollReveal variant="slideLeft"><span>hello</span></ScrollReveal>
       )
       expect(container.firstChild).toBeTruthy()
     })

     it('accepts delay and duration props', () => {
       const { container } = render(
         <ScrollReveal delay={0.5} duration={1}><span>hello</span></ScrollReveal>
       )
       expect(container.firstChild).toBeTruthy()
     })
   })
   ```

   NOTE: The scroll-reveal test is the ONLY one with real test bodies because `ScrollReveal` already exists in the codebase. All other tests use `it.todo()` since their components will be created in Plans 02-01 and 02-02.
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 8 test files exist. Vitest discovers all files, reports todo tests as "todo" (not failures), and scroll-reveal tests pass. Zero test failures.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` discovers all 8 test files
- scroll-reveal tests pass (3 passing)
- All other tests report as "todo" (not failures)
- No import errors from setup file
- Total run time under 10 seconds
</verification>

<success_criteria>
- 8 test files exist in correct directories
- Shared setup mocks motion/react and next/image
- vitest.config.ts references setup file
- `npx vitest run` exits with 0 (todos are not failures)
- scroll-reveal.test.tsx has 3 passing tests
- All other test files have only .todo() stubs
</success_criteria>

<output>
After completion, create `.planning/phases/02-presentation-sections/02-00-SUMMARY.md`
</output>
