---
phase: 02-presentation-sections
plan: 01
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - src/lib/constants.ts
  - src/lib/placeholder-data.ts
  - src/components/ui/stagger-reveal.tsx
  - src/components/sections/section-divider.tsx
  - src/components/sections/hero-section.tsx
  - src/components/sections/hero-countdown.tsx
  - src/components/sections/our-story-section.tsx
  - src/components/sections/story-milestone.tsx
  - src/components/sections/hero-section.test.tsx
  - src/components/sections/hero-countdown.test.tsx
  - src/components/sections/our-story-section.test.tsx
  - src/components/ui/__tests__/scroll-reveal.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HERO-01, HERO-02, CONT-03, ANIM-01]

must_haves:
  truths:
    - "Hero displays couple names in Hindi and English with wedding date"
    - "Countdown timer shows days, hours, minutes, seconds and updates every second"
    - "Our Story section shows 4-5 milestones with placeholder photos in alternating layout"
    - "Scroll-triggered animations reveal sections as user scrolls"
    - "Hindi blessing text appears above couple names in hero"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "Extended EVENTS with time, venue, dressCode, mapUrl, description fields; STORY_MILESTONES array"
      contains: "STORY_MILESTONES"
    - path: "src/lib/placeholder-data.ts"
      provides: "Placeholder images, gallery data, video thumbnail, story photos"
      exports: ["PLACEHOLDER_GALLERY", "PLACEHOLDER_VIDEO", "PLACEHOLDER_STORY_PHOTOS"]
    - path: "src/components/ui/stagger-reveal.tsx"
      provides: "StaggerReveal + StaggerItem for dramatic sequential reveals"
      exports: ["StaggerReveal", "StaggerItem"]
    - path: "src/components/sections/hero-section.tsx"
      provides: "Full-viewport hero with couple names, date, blessing, countdown"
      exports: ["HeroSection"]
    - path: "src/components/sections/hero-countdown.tsx"
      provides: "Client component with live countdown timer in gold-bordered boxes"
      exports: ["HeroCountdown"]
    - path: "src/components/sections/our-story-section.tsx"
      provides: "Timeline section with alternating milestones"
      exports: ["OurStorySection"]
    - path: "src/app/page.tsx"
      provides: "Page composing Hero, Our Story sections with dividers"
  key_links:
    - from: "src/components/sections/hero-section.tsx"
      to: "src/lib/constants.ts"
      via: "imports COUPLE constant"
      pattern: "import.*COUPLE.*from.*constants"
    - from: "src/components/sections/hero-countdown.tsx"
      to: "browser setInterval"
      via: "useEffect with cleanup"
      pattern: "setInterval.*clearInterval"
    - from: "src/components/sections/our-story-section.tsx"
      to: "src/lib/constants.ts"
      via: "imports STORY_MILESTONES"
      pattern: "import.*STORY_MILESTONES.*from.*constants"
    - from: "src/app/page.tsx"
      to: "src/components/sections/"
      via: "imports and composes all sections"
      pattern: "import.*from.*sections"
---

<objective>
Build the data layer, shared animation utilities, hero section with live countdown, and Our Story timeline -- establishing the page skeleton that Plan 02 will fill with remaining sections.

Purpose: Create the top-of-page experience (hero + story) and all shared infrastructure (extended constants, placeholder data, StaggerReveal, section dividers) so Plan 02 can build remaining sections without touching shared files.

Output: Working hero with countdown, Our Story timeline, page.tsx skeleton with section order, placeholder data ready for all sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-presentation-sections/02-CONTEXT.md
@.planning/phases/02-presentation-sections/02-RESEARCH.md
@.planning/phases/02-presentation-sections/02-00-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor needs -->

From src/lib/constants.ts:
```typescript
export const EVENT_COLORS = {
  tilak: '#C41E3A',
  mehndi: '#228B22',
  sangeet: '#9B59B6',
  haldi: '#F4C430',
  shadi: '#800020',
} as const

export const COUPLE = {
  nameHindi: 'योगी & सुधा',
  nameEnglish: 'Yogi & Sudha',
  weddingDate: '28 April 2026',
} as const

export const EVENTS = [
  { name: 'Tilak', nameHindi: 'तिलक', date: '26 April 2026', colorKey: 'tilak' as const },
  { name: 'Mehndi', nameHindi: 'मेहंदी', date: '27 April 2026', colorKey: 'mehndi' as const },
  { name: 'Sangeet', nameHindi: 'संगीत', date: '27 April 2026', colorKey: 'sangeet' as const },
  { name: 'Haldi', nameHindi: 'हल्दी', date: '28 April 2026', colorKey: 'haldi' as const },
  { name: 'Shadi', nameHindi: 'शादी', date: '28 April 2026', colorKey: 'shadi' as const },
] as const
```

From src/components/ui/scroll-reveal.tsx:
```typescript
export function ScrollReveal({
  children,
  variant = 'fadeUp',  // 'fadeUp' | 'fadeIn' | 'slideLeft' | 'slideRight'
  delay = 0,
  duration = 0.6,
  className,
}: ScrollRevealProps)
```

From src/app/globals.css:
```css
@theme {
  --font-hindi, --font-heading, --font-body
  --color-maroon, --color-gold, --color-emerald, --color-cream (+ light/dark variants)
  --color-tilak, --color-mehndi, --color-sangeet, --color-haldi, --color-shadi
  --spacing-section: 6rem; --spacing-section-mobile: 3rem;
}
```

Key Tailwind classes: `font-hindi`, `font-heading`, `font-body`, `text-maroon`, `bg-gold`, `text-gold`, `bg-cream`, `py-section-mobile`, `md:py-section`, `bg-tilak`, `bg-mehndi`, etc.

Animation library: `motion/react` (NOT framer-motion). Project uses `'use client'` only for interactive components.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend data layer and create shared utilities</name>
  <files>src/lib/constants.ts, src/lib/placeholder-data.ts, src/components/ui/stagger-reveal.tsx, src/components/sections/section-divider.tsx</files>
  <action>
**IMPORTANT: MUST use `frontend-design` skill for section-divider component.**

1. **Extend `src/lib/constants.ts`:** Add fields to each EVENTS entry: `time` (string, e.g. "10:00 AM"), `venue` (string, placeholder name), `mapUrl` (string, placeholder Google Maps URL like `https://maps.google.com/?q=Venue+Name+City`), `dressCode` (string, e.g. "Traditional / Kurta Pajama"), `description` (string, 1-2 line explanation of the tradition). Add a new `STORY_MILESTONES` array with 5 objects: `{ id: string, title: string, titleHindi: string, year: string, description: string, imageKey: string }`. Add `WEDDING_TARGET_DATE = '2026-04-28T00:00:00+05:30'` constant. Keep existing COUPLE and EVENT_COLORS untouched.

    Event details (all placeholder -- clearly commented as such):
    - Tilak: 26 Apr 2026, 10:00 AM, "Venue TBD, City", "Traditional / Kurta Pajama", "The groom is formally welcomed and honored by the bride's family with a sacred tilak ceremony."
    - Mehndi: 27 Apr 2026, 4:00 PM, "Venue TBD, City", "Colorful / Lehenga or Saree", "An evening of intricate henna art, music, and celebration as the bride is adorned with beautiful mehndi designs."
    - Sangeet: 27 Apr 2026, 7:00 PM, "Venue TBD, City", "Glamorous / Party Wear", "A night of dance, music, and Bollywood performances celebrating the union of two families."
    - Haldi: 28 Apr 2026, 9:00 AM, "Venue TBD, City", "Yellow / Casual Indian", "A joyous morning ritual where turmeric paste is applied to bless the couple with radiance and prosperity."
    - Shadi: 28 Apr 2026, 7:00 PM, "Venue TBD, City", "Grand / Sherwani or Saree", "The sacred wedding ceremony where two souls unite in the presence of family, friends, and divine blessings."

    Story milestones (placeholder):
    - "How We Met" (2020), "First Adventure Together" (2021), "The Moment We Knew" (2022), "The Proposal" (2024), "Beginning Forever" (2026)

2. **Create `src/lib/placeholder-data.ts`:** Export `PLACEHOLDER_GALLERY` (array of 10 objects with `id`, `src` using `/api/placeholder/{width}/{height}` URLs or gradient CSS-based placeholders, `alt`, `width`, `height` with varied aspect ratios -- portrait, landscape, square). Export `PLACEHOLDER_VIDEO` with `thumbnailSrc` and `videoUrl` (YouTube embed URL placeholder). Export `PLACEHOLDER_STORY_PHOTOS` mapping milestone IDs to placeholder image paths. NOTE: For placeholder images, use simple colored div placeholders or generate inline SVG data URIs with gold/maroon/cream gradients -- do NOT rely on external placeholder services.

3. **Create `src/components/ui/stagger-reveal.tsx`:** Client component (`'use client'`). Import `motion` from `motion/react`. Create `StaggerReveal` (parent container with `staggerChildren: 0.15, delayChildren: 0.1`, `whileInView="visible"`, `viewport={{ once: true, margin: '-80px' }}`). Create `StaggerItem` (child with `hidden: { opacity: 0, y: 50 }` to `visible: { opacity: 1, y: 0 }` over 0.6s easeOut). Export both. Follow the exact pattern from RESEARCH.md Pattern 3.

4. **Create `src/components/sections/section-divider.tsx`:** Server component. Render a decorative gold divider with a small mandala/paisley SVG motif centered between two gradient lines (transparent-to-gold). Keep SVG inline and under 2KB. Use `text-gold` for the SVG fill. Add `py-8` spacing. This is a pure decorative element used between sections.
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npx tsc --noEmit 2>&1 | head -30 && npx vitest run src/components/ui/__tests__/scroll-reveal.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Constants extended with full event details and story milestones. Placeholder data module exists. StaggerReveal exports both components. SectionDivider renders gold mandala divider. All files type-check cleanly. Existing scroll-reveal tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build hero section with countdown and Our Story timeline, fill in test stubs</name>
  <files>src/components/sections/hero-section.tsx, src/components/sections/hero-countdown.tsx, src/components/sections/our-story-section.tsx, src/components/sections/story-milestone.tsx, src/app/page.tsx, src/components/sections/__tests__/hero-section.test.tsx, src/components/sections/__tests__/hero-countdown.test.tsx, src/components/sections/__tests__/our-story-section.test.tsx</files>
  <action>
**IMPORTANT: MUST use `frontend-design` skill for ALL component creation. This is a HARD CONSTRAINT from the user -- design quality over speed. Every component must be built through the frontend-design skill.**

1. **Create `src/components/sections/hero-section.tsx`:** Server component (no `'use client'`). Full-viewport height (`min-h-screen`). Background: placeholder gradient (deep maroon to dark, simulating a couple photo background with dark overlay -- use `bg-gradient-to-b from-maroon-dark/90 via-maroon/70 to-maroon-dark/80`). Content centered vertically and horizontally. Render in order: Hindi blessing "शुभ विवाह" in `font-hindi text-2xl md:text-3xl text-gold-light`, then couple Hindi names from `COUPLE.nameHindi` in `font-hindi text-6xl md:text-8xl lg:text-9xl text-gold`, then English names from `COUPLE.nameEnglish` in `font-heading text-3xl md:text-5xl text-white`, then wedding date in `font-body text-xl text-cream`, then `<HeroCountdown targetDate={WEDDING_TARGET_DATE} />`. Add ornate gold border or decorative frame around the content. Add `overflow-hidden` for any particle effects. All text should have subtle text-shadow for readability over the background.

2. **Create `src/components/sections/hero-countdown.tsx`:** Client component (`'use client'`). Accept `targetDate: string` prop. Use `getTimeRemaining` helper function (as in RESEARCH.md Pattern 2). Initialize state with `getTimeRemaining(targetDate)`. Set up `useEffect` with `setInterval` every 1000ms, with proper cleanup. Handle hydration mismatch: use `suppressHydrationWarning` on the container div, OR use a `mounted` state flag to show placeholder on server. Render 4 decorative boxes (days, hours, minutes, seconds), each with `border-2 border-gold rounded-lg p-3 md:p-4 min-w-[70px] md:min-w-[80px] bg-maroon/30 backdrop-blur-sm`. Large numbers in `font-heading text-3xl md:text-4xl text-gold`. Small labels in `font-body text-xs md:text-sm text-cream/80 uppercase`. Labels should be: "Days", "Hours", "Minutes", "Seconds".

3. **Create `src/components/sections/our-story-section.tsx`:** Server component. Section with `py-section-mobile md:py-section`. Section heading: "Our Story" in `font-heading` + "हमारी कहानी" in `font-hindi` as subheading. Import `STORY_MILESTONES` from constants and `PLACEHOLDER_STORY_PHOTOS` from placeholder-data. Render a vertical timeline: gold center line (`w-0.5 bg-gold` absolute positioned). For each milestone, render `<StoryMilestone>` wrapped in `<ScrollReveal>` with alternating `slideLeft`/`slideRight` variants. On mobile (below `md:`), show single-column layout with all milestones left-aligned. On desktop, alternate left/right using flex-direction or translate.

4. **Create `src/components/sections/story-milestone.tsx`:** Server component. Accept milestone data + `position: 'left' | 'right'` + placeholder image source. Render: circular image placeholder (clipped with `rounded-full`), year badge, title (English), description. Use the event accent colors or gold for decorative elements. Card-style with subtle shadow and cream background. Thick gold dot on the timeline connector point.

5. **Replace `src/app/page.tsx`:** Remove ALL existing demo content. Compose sections in this order: `<HeroSection />`, `<SectionDivider />`, `<OurStorySection />`, `<SectionDivider />`, then placeholder comments for Events, Gallery, Video sections (Plan 02 will add these), then a placeholder `<section>` for RSVP (Phase 3). Import all section components. Keep as server component (no `'use client'`).

6. **Fill in Wave 0 test stubs:** Now that the components exist, update the test stub files created by Plan 02-00:
   - **`src/components/sections/__tests__/hero-section.test.tsx`:** Uncomment imports, replace `it.todo()` with real tests. Test that "शुभ विवाह" renders, couple Hindi name renders, couple English name renders, wedding date renders.
   - **`src/components/sections/__tests__/hero-countdown.test.tsx`:** Uncomment imports, replace `it.todo()` with real tests. Test that 4 countdown boxes render (days, hours, minutes, seconds labels), that correct values display for a known future date (mock Date.now), that cleanup runs on unmount.
   - **`src/components/sections/__tests__/our-story-section.test.tsx`:** Uncomment imports, replace `it.todo()` with real tests. Test that section heading "Our Story" renders, Hindi heading renders, all 5 milestones render (check for milestone titles).
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npx vitest run src/components/sections/__tests__/hero-section.test.tsx src/components/sections/__tests__/hero-countdown.test.tsx src/components/sections/__tests__/our-story-section.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Hero section renders with Hindi blessing, couple names in Hindi + English, wedding date, and live countdown timer with gold-bordered boxes. Our Story section shows 5 milestones in alternating timeline layout with scroll-triggered reveals. Page composes Hero > Divider > Our Story > Divider with placeholder spots for remaining sections. All 3 test files pass with real assertions.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests (hero-section, hero-countdown, our-story-section, scroll-reveal)
- `npx tsc --noEmit` passes type checking
- Hero section is visible at page top with full viewport height
- Countdown timer boxes display and update every second
- Our Story timeline shows alternating milestones with scroll animations
- Section dividers appear between sections with gold mandala motif
</verification>

<success_criteria>
- Hero section displays "शुभ विवाह", couple names in Hindi + English, wedding date, and live countdown
- Countdown shows 4 gold-bordered boxes (days/hours/minutes/seconds) updating every second
- Our Story shows 5 placeholder milestones in alternating left/right timeline
- ScrollReveal animations trigger on scroll for story milestones
- Page follows section order: Hero > Our Story > (placeholder for Events, Gallery, Video, RSVP)
- All placeholder content is clearly marked for easy replacement
- Unit tests for hero-section, hero-countdown, and our-story-section pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-presentation-sections/02-01-SUMMARY.md`
</output>
