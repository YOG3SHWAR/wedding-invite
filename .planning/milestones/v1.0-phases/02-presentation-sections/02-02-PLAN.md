---
phase: 02-presentation-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/event-timeline.tsx
  - src/components/sections/event-card.tsx
  - src/components/sections/photo-gallery.tsx
  - src/components/sections/video-section.tsx
  - src/components/sections/__tests__/event-timeline.test.tsx
  - src/components/sections/__tests__/event-card.test.tsx
  - src/components/sections/__tests__/photo-gallery.test.tsx
  - src/components/sections/__tests__/video-section.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [EVNT-01, EVNT-02, EVNT-03, CONT-01, CONT-04, ANIM-01]

must_haves:
  truths:
    - "All 5 events (Tilak, Mehndi, Sangeet, Haldi, Shadi) display with date, time, venue, dress code, and Google Maps link"
    - "Each event card has a distinct accent color matching its event identity"
    - "Photo gallery shows masonry grid with images that open in lightbox on click"
    - "Gallery images animate in with staggered back-and-forth motion on scroll"
    - "Video section shows cinematic thumbnail with gold play button that loads iframe on click"
    - "All images in gallery are lazy-loaded"
  artifacts:
    - path: "src/components/sections/event-timeline.tsx"
      provides: "Vertical timeline with gold line and alternating event cards"
      exports: ["EventTimeline"]
    - path: "src/components/sections/event-card.tsx"
      provides: "Individual event card with accent color border, all details, map link"
      exports: ["EventCard"]
    - path: "src/components/sections/photo-gallery.tsx"
      provides: "Masonry grid with lightbox integration and stagger animations"
      exports: ["PhotoGallery"]
    - path: "src/components/sections/video-section.tsx"
      provides: "Click-to-load video embed with cinematic thumbnail"
      exports: ["VideoSection"]
    - path: "src/app/page.tsx"
      provides: "Complete page with all sections in order"
  key_links:
    - from: "src/components/sections/event-timeline.tsx"
      to: "src/lib/constants.ts"
      via: "imports EVENTS array with full details"
      pattern: "import.*EVENTS.*from.*constants"
    - from: "src/components/sections/event-card.tsx"
      to: "src/lib/constants.ts"
      via: "uses EVENT_COLORS for accent styling"
      pattern: "EVENT_COLORS|colorKey"
    - from: "src/components/sections/photo-gallery.tsx"
      to: "yet-another-react-lightbox"
      via: "Lightbox component for fullscreen view"
      pattern: "import.*Lightbox.*from.*yet-another-react-lightbox"
    - from: "src/components/sections/video-section.tsx"
      to: "iframe embed"
      via: "click handler replaces thumbnail with iframe"
      pattern: "iframe.*autoplay"
    - from: "src/app/page.tsx"
      to: "all section components"
      via: "imports and composes in order"
      pattern: "EventTimeline|PhotoGallery|VideoSection"
---

<objective>
Build the event timeline, photo gallery with lightbox, and pre-wedding video section -- completing all visual content sections for the wedding site.

Purpose: Deliver the remaining 3 content sections (events, gallery, video) so the complete scrollable wedding experience is viewable end-to-end.

Output: Event timeline with 5 color-coded cards, masonry photo gallery with lightbox, click-to-load video embed, and fully composed page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-presentation-sections/02-CONTEXT.md
@.planning/phases/02-presentation-sections/02-RESEARCH.md
@.planning/phases/02-presentation-sections/02-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes -->

From src/lib/constants.ts (extended by Plan 01):
```typescript
export const WEDDING_TARGET_DATE = '2026-04-28T00:00:00+05:30'

export const EVENTS = [
  {
    name: 'Tilak', nameHindi: 'तिलक', date: '26 April 2026',
    time: '10:00 AM', venue: 'Venue TBD, City',
    mapUrl: 'https://maps.google.com/?q=Venue+Name+City',
    dressCode: 'Traditional / Kurta Pajama',
    description: '...', colorKey: 'tilak' as const
  },
  // ... 4 more events with same shape
] as const

export const EVENT_COLORS = {
  tilak: '#C41E3A', mehndi: '#228B22', sangeet: '#9B59B6',
  haldi: '#F4C430', shadi: '#800020',
} as const
```

From src/lib/placeholder-data.ts (created by Plan 01):
```typescript
export const PLACEHOLDER_GALLERY: { id: string; src: string; alt: string; width: number; height: number }[]
export const PLACEHOLDER_VIDEO: { thumbnailSrc: string; videoUrl: string }
```

From src/components/ui/stagger-reveal.tsx (created by Plan 01):
```typescript
export function StaggerReveal({ children, className }: { children: ReactNode; className?: string })
export function StaggerItem({ children, className }: { children: ReactNode; className?: string })
```

From src/components/ui/scroll-reveal.tsx (existing):
```typescript
export function ScrollReveal({ children, variant, delay, duration, className }: ScrollRevealProps)
// variants: 'fadeUp' | 'fadeIn' | 'slideLeft' | 'slideRight'
```

From src/components/sections/section-divider.tsx (created by Plan 01):
```typescript
export function SectionDivider()
```

Tailwind tokens: `bg-tilak`, `bg-mehndi`, `bg-sangeet`, `bg-haldi`, `bg-shadi`, `border-gold`, `text-gold`, `font-hindi`, `font-heading`, `font-body`, `py-section-mobile md:py-section`

Animation library: `motion/react` (NOT framer-motion)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build event timeline with color-coded cards and fill in test stubs</name>
  <files>src/components/sections/event-timeline.tsx, src/components/sections/event-card.tsx, src/components/sections/__tests__/event-timeline.test.tsx, src/components/sections/__tests__/event-card.test.tsx</files>
  <action>
**IMPORTANT: MUST use `frontend-design` skill for ALL component creation. This is a HARD CONSTRAINT -- non-negotiable.**

First, install the lightbox dependency needed for Task 2:
```bash
npm install yet-another-react-lightbox
```

1. **Create `src/components/sections/event-card.tsx`:** Server component. Accept event object (matching the extended EVENTS type from constants) + `position: 'left' | 'right'` prop. Render a card with:
   - Thick left border (4px) in the event's accent color using the `colorKey` to apply the matching Tailwind border color class (e.g., `border-l-4 border-l-tilak` for tilak). Use inline style with `EVENT_COLORS[event.colorKey]` for the border color since dynamic Tailwind classes may not work.
   - Card background: `bg-cream-dark` with subtle shadow
   - Event name in Hindi (`font-hindi text-2xl text-gold`) and English (`font-heading text-xl`) stacked
   - Date and time prominently displayed with a calendar-style icon (inline SVG)
   - Venue with a map pin icon (inline SVG) + Google Maps link (`<a href={event.mapUrl} target="_blank" rel="noopener noreferrer">` styled as gold underlined text)
   - Dress code with a small clothing icon or decorative text label
   - Short description in `font-body text-sm text-maroon-dark/80`
   - On mobile: full-width card, no position alternation
   - On desktop: card takes ~45% width, positioned left or right based on `position` prop

2. **Create `src/components/sections/event-timeline.tsx`:** Server component. Section heading: "Wedding Events" in `font-heading` + "शुभ कार्यक्रम" in `font-hindi`. Render vertical timeline: absolute-positioned gold center line (`w-0.5 bg-gold` on desktop, left-aligned `left-4` on mobile). Import `EVENTS` from constants. Map over events, alternating `position` (left/right). Wrap each `<EventCard>` in `<ScrollReveal>` with alternating `slideLeft`/`slideRight` variants. Add gold dot markers on the timeline at each event's position. On mobile (below `md:`): single-column, timeline line on the left, all cards on the right.

3. **Fill in test stubs for event components:**
   - **`src/components/sections/__tests__/event-timeline.test.tsx`:** Uncomment imports, replace `it.todo()` with real tests. Test that all 5 event names render (check for "Tilak", "Mehndi", "Sangeet", "Haldi", "Shadi"), section heading "Wedding Events" renders, Hindi heading renders.
   - **`src/components/sections/__tests__/event-card.test.tsx`:** Uncomment imports, replace `it.todo()` with real tests. Test that event name in Hindi and English renders, date and time render, venue with Maps link renders (check for `<a>` with correct href), dress code text renders, description renders, accent color border is applied (check inline style or class).
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npx vitest run src/components/sections/__tests__/event-timeline.test.tsx src/components/sections/__tests__/event-card.test.tsx --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>Event timeline renders 5 events with distinct accent-colored left borders. Each card shows Hindi + English name, date, time, venue with Maps link, dress code, and description. Cards alternate left/right on desktop, stack on mobile. ScrollReveal animates each card on scroll. All event-timeline and event-card tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build photo gallery with lightbox and video section, compose final page, fill in test stubs</name>
  <files>src/components/sections/photo-gallery.tsx, src/components/sections/video-section.tsx, src/app/page.tsx, src/components/sections/__tests__/photo-gallery.test.tsx, src/components/sections/__tests__/video-section.test.tsx</files>
  <action>
**IMPORTANT: MUST use `frontend-design` skill for ALL component creation. HARD CONSTRAINT.**

1. **Create `src/components/sections/photo-gallery.tsx`:** Client component (`'use client'`). Import `Lightbox` from `yet-another-react-lightbox` and its CSS (`yet-another-react-lightbox/styles.css`). Import `PLACEHOLDER_GALLERY` from placeholder-data. Import `StaggerReveal`, `StaggerItem` from stagger-reveal. Section heading: "Gallery" in `font-heading` + "तस्वीरें" in `font-hindi`.

   Masonry layout using CSS columns: `columns-2 md:columns-3 lg:columns-4 gap-4 space-y-4`. Each image in a `break-inside-avoid` container. Use `<div>` placeholders with colored gradients (gold/maroon/cream variations) for placeholder images since real photos aren't available yet -- each placeholder should have the correct aspect ratio from PLACEHOLDER_GALLERY data. Wrap the entire grid in `<StaggerReveal>` and each image in `<StaggerItem>`. For the "back-and-forth" scroll animation effect the user requested: alternate StaggerItem transform origins or use custom variants where even items slide from left and odd items slide from right.

   Lightbox state: `open` (boolean) and `index` (number). Click on any image sets index and opens lightbox. Render `<Lightbox>` component with `open`, `close`, `index`, and `slides` props.

   All images should use `loading="lazy"` when real images are used. For placeholder divs, this is N/A.

2. **Create `src/components/sections/video-section.tsx`:** Client component (`'use client'`). Import `PLACEHOLDER_VIDEO` from placeholder-data. Section heading: "Our Film" in `font-heading` + "हमारी फिल्म" in `font-hindi`. Show cinematic thumbnail (placeholder gradient div with film-strip decorative elements). Centered dramatic gold play button: `w-20 h-20 rounded-full bg-gold/90` with white play triangle SVG, `group-hover:scale-110 transition-transform duration-300`. On click, replace thumbnail with `<iframe>` loading the video URL with `?autoplay=1`. Include `allow="autoplay; fullscreen"` and `allowFullScreen` on iframe. Wrap in `aspect-video max-w-4xl mx-auto`. Add `rounded-xl overflow-hidden` for polished look.

3. **Update `src/app/page.tsx`:** Add the new sections to the page composition. Final order MUST be:
   - `<HeroSection />`
   - `<SectionDivider />`
   - `<OurStorySection />`
   - `<SectionDivider />`
   - `<EventTimeline />`
   - `<SectionDivider />`
   - `<PhotoGallery />`
   - `<SectionDivider />`
   - `<VideoSection />`
   - `<SectionDivider />`
   - RSVP placeholder section (simple centered text: "RSVP Coming Soon" in `font-heading text-2xl text-maroon` with `py-section-mobile md:py-section`)

   Import all section components. Keep page.tsx as server component.

4. **Fill in test stubs for gallery and video components:**
   - **`src/components/sections/__tests__/photo-gallery.test.tsx`:** Uncomment imports, replace `it.todo()` with real tests. Test that gallery items render (check for placeholder elements), lightbox opens on click (simulate click, check for lightbox open state), section heading "Gallery" renders.
   - **`src/components/sections/__tests__/video-section.test.tsx`:** Uncomment imports, replace `it.todo()` with real tests. Test that thumbnail with play button renders initially, no iframe exists before click, clicking play button renders iframe with autoplay param, section heading "Our Film" renders.
  </action>
  <verify>
    <automated>cd /Users/yogeshwarchaturvedi/Documents/yogi/wedding-invite && npx vitest run src/components/sections/__tests__/photo-gallery.test.tsx src/components/sections/__tests__/video-section.test.tsx --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>Photo gallery renders masonry grid with placeholder images, lightbox opens on click, stagger animations alternate direction. Video section shows thumbnail with gold play button, replaces with iframe on click. Page composes all 5 sections in correct order with dividers. All photo-gallery and video-section tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests (all 8 test files)
- `npx tsc --noEmit` passes type checking
- All 5 events render in timeline with distinct accent colors
- Gallery shows masonry layout with lightbox functionality
- Video thumbnail shows gold play button
- Page section order: Hero > Our Story > Events > Gallery > Video > RSVP placeholder
- All scroll-triggered animations work (ScrollReveal + StaggerReveal)
</verification>

<success_criteria>
- 5 event cards each show: Hindi + English name, date, time, venue + Maps link, dress code, description
- Each event card has its distinct accent color (tilak=red, mehndi=green, sangeet=purple, haldi=yellow, shadi=maroon)
- Timeline alternates left/right on desktop, single-column on mobile
- Gallery displays masonry grid with back-and-forth stagger animations
- Lightbox opens when clicking gallery image
- Video section shows thumbnail + gold play button, loads iframe on click
- Complete page scrolls through all sections with smooth animations
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-presentation-sections/02-02-SUMMARY.md`
</output>
